# Copy to .deploy.env (git-ignored) and fill in. The repository is public, so the
# board's address, login and password live here and never in a tracked file.
BOARD_HOST=
BOARD_USER=
# Leave empty to use ssh key auth (preferred: ssh-copy-id <user>@<board>).
BOARD_PASS=
